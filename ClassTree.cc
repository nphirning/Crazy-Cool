// File         : ClassTree.cc
// Description  : Implemention of ClassTree class for generating class structure.

#include <string>
#include <vector>
#include <map>
#include <set>

using namespace std;

ClassTree::ClassTree(int num_classes = "", string word_corpus = "") {
  this->num_classes = num_classes;
  this->word_corpus = word_corpus
}

void generate_class_tree() {
  
}
